CREATE TABLE DataSources (
    identifier TEXT NOT NULL UNIQUE,
    description TEXT
);

insertDataSource:
INSERT INTO DataSources(identifier, description)
VALUES(?, ?);

selectAll:
SELECT * FROM DataSources;